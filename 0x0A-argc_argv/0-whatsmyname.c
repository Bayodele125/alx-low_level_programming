#include <stdlib.h>
#include <stdio.h>

/**
* main - prints its name, followed by a new line.
* @argc: The lenght of the command line arg
*@argv: Array of argc
* Return: Always (0)
*/

int main(int argc, char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
